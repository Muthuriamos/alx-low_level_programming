#include <stdio.h>

/**
 *main -> a random number n each time its executed
 *prints the last digit of the number stored in the variable n
 *Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
	printf("%d", n);
	}
	printf("\n");
	return (0);
}
